let point: [number, number]

point = [2, 5]

let [x, y] = point

let exemploTupla: [string, number, boolean];

exemploTupla = ['Texto', 42, true];

let primeiroElemento: string = exemploTupla[0];
let segundoElemento: number = exemploTupla[1];
let terceiroElemento: boolean = exemploTupla[2];

